@model List<string>
@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    <ul class="float-right">
        @foreach (var pais in Model)
        {
            <li >@pais</li>
        }
    </ul>
</div>
